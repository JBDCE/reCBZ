# Unit testing

To execute the tests provided in this repository you can run this command in
the root of the project after installing it
as outlined in the build from source instructions in the root readme file:

`coverage run -m --source=src/reCBZ unittest discover -s tests; coverage report`
